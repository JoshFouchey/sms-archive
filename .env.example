# Example environment variables for frontend container CSP
# Space-separated list of additional connect-src endpoints (protocol + host, include wss: if needed)
# e.g. CSP_CONNECT_SRC="https://api-messages.joshfouchey.win wss://api-messages.joshfouchey.win"
CSP_CONNECT_SRC=

# Vite build-time API base (used at build stage for SPA to call backend)
VITE_API_BASE=/api


