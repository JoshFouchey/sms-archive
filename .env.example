# Copy to .env and adjust for your server paths

# Port Configuration (adjust these to avoid conflicts with other containers)
DB_PORT=5432
APP_PORT=8080
FRONTEND_PORT=80

# Database Configuration
DB_VERSION=15-alpine
DB_NAME=sms_archive
DB_USER=sms_user
DB_PASSWORD=sms_pass

# Volume Paths
DB_DATA_HOST_PATH=/srv/sms-archive/postgres
MEDIA_ROOT_HOST_PATH=/srv/sms-archive/media/messages

# Application Configuration
APP_BASE_URL=https://your-domain.example
LOG_LEVEL=INFO
TZ=UTC

# Frontend build-time API base override (leave empty unless reverse proxy requires prefix)
VITE_API_BASE=

# Optional: if you want a different relative path inside container, leave SMSARCHIVE_MEDIA_ROOT as set in compose.
# SMSARCHIVE_MEDIA_ROOT is set via docker-compose.yml environment section for the app service.
